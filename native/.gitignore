# Capacitor / Android build outputs — regenerated by `npx cap add android` + Gradle.
node_modules/
android/
ios/
.gradle/
dist/
*.apk
*.aab

# Signing material — NEVER commit keystores or passwords.
*.keystore
*.jks
keystore.properties
release-signing.properties
